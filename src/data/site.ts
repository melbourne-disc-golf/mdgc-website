// Shared site-wide constants and external URLs.

// The club's online store (Square Online). Linked directly so off-site links
// get the external treatment (new-tab + cue). The `/shop` redirect in
// astro.config.mjs remains as a convenience alias, but isn't load-bearing.
export const shopUrl = 'https://mdgcshop.square.site/';
