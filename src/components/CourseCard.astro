---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

export interface Props {
  name: string;
  href: string;
  location: string;
  image: ImageMetadata;
}

const { name, href, location, image } = Astro.props;
---

<a href={href} class="course-card">
  <div class="course-card-inner">
    <Image
      src={image}
      alt={name}
      class="course-card-image"
      inferSize={true}
    />
    <div class="course-card-content">
      <h3 class="course-card-title">
        {name}
      </h3>
      <p class="course-card-location">{location}</p>
    </div>
  </div>
</a>

<style>
  .course-card {
    text-decoration: none;
    color: inherit;
    display: block;
    height: 100%;
  }

  .course-card-inner {
    background-color: white;
    border: 1px solid #d7e7f1;
    border-radius: 0.5rem;
    overflow: hidden;
    transition: border-color 0.18s ease, box-shadow 0.18s ease, transform 0.18s ease;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .course-card:hover .course-card-inner {
    border-color: var(--color-mdgc-accent);
    box-shadow: 0 14px 30px rgba(6, 61, 99, 0.12);
    transform: translateY(-2px);
  }

  .course-card-image {
    width: 100%;
    height: 12rem;
    object-fit: cover;
    flex-shrink: 0;
  }

  .course-card-content {
    padding: 0.85rem 1rem 1rem;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
  }

  .course-card-title {
    font-size: 1.125rem;
    font-weight: 700;
    margin-top: 0.25rem;
    margin-bottom: 0;
    color: var(--color-mdgc-ink);
    transition: color 0.2s;
  }

  .course-card:hover .course-card-title {
    color: var(--color-mdgc-accent-deep);
  }

  .course-card-location {
    color: #5e6d78;
    margin: 0;
  }
</style>
